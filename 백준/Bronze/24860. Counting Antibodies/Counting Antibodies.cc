#include <iostream>
using std::cin;
using std::cout;

int main()
{
    long long int vk, jk, va, ja, vh, dh, jh;
    cin >> vk >> jk >> va >> ja >> vh >> dh >> jh;
    cout<<(vh * dh * jh)*((vk * jk)+(va * ja));
    return 0;
}